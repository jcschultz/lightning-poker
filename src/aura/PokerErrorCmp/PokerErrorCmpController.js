/**
 * Created by jschultz on 6/5/17.
 */
({
	
	close : function(component, event, helper) {
		helper.closeAlert(component, event, helper);
	},
	
	handleToggle : function(component, event, helper) {
		helper.toggleAlert(component, event, helper);
	},
	
})